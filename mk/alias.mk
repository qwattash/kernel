#
# This file lists all the public functions available in anrem.
# The user can uncomment and define an alias name for each of
# those functions so that they can be call more easily.
#

#optarg = $(call anrem-optarg, $1, $2)
#list-reverse = $(call anrem-list-reverse, $1)
#current-path = $(call anrem-current-path)
#mod-var = $(call anrem-mod-var, $1, $2)
#mod-exclude = $(call anrem-mod-exclude, $1)
#build = $(call anrem-build, $1)
#clean = $(call anrem-clean, $1)
#test = $(call anrem-test, $1)
#target = $(call anrem-target, $1)
#build-list-add = $(call anrem-build-list-add, $1) 
#clean-list-add = $(call anrem-clean-list-add, $1)
#test-list-add = $(call anrem-test-list-add, $1)
#target-def-var = $(call anrem-target-def-var, $1, $2, $3)
#path-cut = $(call anrem-path-cut, $1, $2)
#local = $(call anrem-local, $1)
#local-set = $(call anrem-local-set, $1, $2) 
#local-get = $(call anrem-local-get, $1)
#join = $(call anrem-join, $1)
#auto-target = $(call anrem-auto-target, $1, $2, $3, $4, $5)
#target-group-add = $(call anrem-target-group-add, $1, $2)
#target-group-depend = $(call anrem-target-group-depend, $1)
#target-group-members = $(call anrem-target-group-members, $1)
#target-group-modules = $(call anrem-target-group-modules, $1)
